# Netlify CLI 指令速查

```bash
netlify status
netlify login
netlify logout
netlify init
netlify link
netlify link --id <site-id>
netlify deploy --dir=dist
netlify deploy --dir=dist --prod
netlify env:list
netlify env:set KEY "value"
netlify build --clear
netlify rollback
```
